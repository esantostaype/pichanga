import { MatchScreen } from "@/components/layout/match-screen";

export const dynamic = "force-dynamic";

/** The match that owns the moment: live, settling up, or the next one. */
export default function HomePage() {
  return <MatchScreen />;
}
